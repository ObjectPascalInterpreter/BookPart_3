
a = {}
length = random.randint (50)

for i = 0 to length - 1 do
  lists.append (a, random.randint (20))
end

println (a)

sum = 0
for i = 0 to length - 1 do
    sum = sum + a[i]
end

println ("Sum = ", sum)

