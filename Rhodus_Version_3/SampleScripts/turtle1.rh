

tt.setPosition (0,0)
tt.clear()
tt.penDown ();
while True do
  tt.forward (200);
  tt.left (170);
  p = tt.getPosition();

  d = math.sqrt(p[0]*p[0] + p[1]*p[1]);
  if d < 1 then
     break
   end
end
