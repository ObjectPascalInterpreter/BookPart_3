
tt.reset()
tt.clear()

tt.bk (tt.width()/2)
tt.fd (2*tt.width())

tt.setpos(0,0)
tt.lt (90)
tt.fd (tt.height()/2)
tt.rt(180)
tt.fd (tt.height())

tt.reset()
for i = 1 to 8 do
  //tt.pencolor ("gray")
  tt.fd (20*i)
  tt.rt(90)
  tt.fd (200)
  tt.rt (180)
  tt.fd (400)
  tt.reset()
end