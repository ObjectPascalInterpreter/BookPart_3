

a = {}
for i = 0 to 30 do
  lists.append (a, random.randint(20))
end

// Find largest number in a
max = a[0]

for i = 0 to 30 do
    if arr[i] > max then
       max = arr[i]
    end
end

println ("Largest number = ", max)